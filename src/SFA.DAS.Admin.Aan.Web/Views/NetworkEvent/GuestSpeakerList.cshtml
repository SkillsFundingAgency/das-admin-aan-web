@using SFA.DAS.Admin.Aan.Web.Infrastructure
@model SFA.DAS.Admin.Aan.Web.Models.NetworkEvent.CreateEventGuestSpeakerListViewModel
@{
    ViewData["Title"] = "Create Calendar Event";
}

<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        <h1 class="govuk-heading-l">
            <span class="govuk-caption-l">Create event</span>
            Guest Speaker
        </h1>
        <form asp-route="@RouteNames.CreateEvent.GuestSpeakerList" method="POST">
            <div class="govuk-form-group">
                <input type="hidden" asp-for="@Model.BackLink"/>
                <div class="govuk-form-group" >
                    <div id="GuestSpeakers" class="">
                        @foreach (var gs in Model.GuestSpeakers)
                        {
                        }
                    </div>
                </div>
                <hr class="govuk-section-break govuk-section-break--l govuk-section-break--visible">
                <a href="_self" class="govuk-link">Add Speaker</a>
                    <hr class="govuk-section-break govuk-section-break--l govuk-section-break--visible">
                    <div class="govuk-button-group">
                        <button type="submit" id="continue" class="govuk-button" data-disable-on-submit="true">Continue</button>
                        <a href="@Model.BackLink" class="govuk-link">Cancel</a>
                    </div>
            </div>
        </form>
    </div>
</div>